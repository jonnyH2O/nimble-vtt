# Tasks

- [x] Add Tooltip component to Undo button in Toolbar (`Ctrl+Z`) <!-- id: 0 -->
- [x] Add Tooltip component to Redo button in Toolbar (`Ctrl+Shift+Z`) <!-- id: 1 -->

## Review
- Added `Tooltip` component to Undo and Redo buttons in `Toolbar.jsx`.
- Configured correct keyboard shortcuts: `Ctrl+Z` for Undo and `Ctrl+Shift+Z` for Redo.
- Removed native `title` attributes to avoid duplicate tooltips.
